using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Notes : MonoBehaviour
{
    /*
     If there is an error with code not running, make sure that all variables are assigned

     When you add an entity to the stage, you have to make sure that the tileset variables are assigned
     */
    public string[] notes;
}
